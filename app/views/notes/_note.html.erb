<li>  
  <%= image_for(note.user) %>
  <span class="user-name">
    <%= note.user.name+"さん" %>
  </span>
  <span class="content">
    <%= link_to note.title, note_path(note.id) %>

  </span>
  <span>
    <%= note.created_at.strftime("%Y-%m-%d") %>
  </span>
    

</li>


